import React from "react";

//style
import { Button } from "./blocks";

export const Main = () => {
  return (
    <div>
      <Button bg="yellow" p={3} color="blue" borderRadius={2}>
        hey
      </Button>
    </div>
  );
};
